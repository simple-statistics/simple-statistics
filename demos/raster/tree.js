function tree() {
    var t = {};

    // Train the classifier tree.
    t.train = function() {
        return t;
    };

    // Add a datapoint and its resulting decision
    t.add = function(x, result) {
        return t;
    };

    // Get the result of a tree for a value
    t.get = function(x) {
    };

    return t;
}
